# Ignore node_modules in root and frontend
/node_modules/
/frontend/node_modules/
.idea
# Ignore env files
.env
.env.local
# Ignore all files in uploads folder but keep the folder structure
# /uploads/*
# Keep the uploads folder itself
# !/uploads/.gitkeep

.idea
